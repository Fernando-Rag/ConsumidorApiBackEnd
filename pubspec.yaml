name: cosumidorapi
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0  # Para peticiones HTTP
  flutter_secure_storage: ^9.0.0  # Para almacenar tokens de forma segura
  provider: ^6.1.0  # Para manejo de estado (opcional pero recomendado)

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
